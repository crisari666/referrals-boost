/** Matches `UserLevel.admin` in omega_office_back `user.schema`. */
export const USER_LEVEL_ADMIN = 0;

/** Matches `UserLevel.subadmin` (coordinator) in omega_office_back `user.schema`. */
export const USER_LEVEL_SUBADMIN = 1;

/** Matches `UserLevel.commercialDirector` (main lead in ventor apps). */
export const USER_LEVEL_MAIN_LEAD = 2;

/** Matches `UserLevel.ventor` in omega_office_back `user.schema`. */
export const USER_LEVEL_VENTOR = 4;

/** Matches `UserLevel.externalAgent` in omega_office_back `user.schema`. */
export const USER_LEVEL_EXTERNAL_AGENT = 10;
